apply from: '../metl-assemble/common.gradle'

dependencies {
    compileOnly project(':metl-core')   
    api "org.bouncycastle:bcprov-jdk18on:${bouncycastleVersion}"
    testImplementation project(path: ':metl-core', configuration: 'testArtifacts')     
}

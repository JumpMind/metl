description=h2

connectionSpec=jdbc:h2:build/dbs/testdb

db=path to database or :memory:

driver=org.h2.Driver

# Sample path to the SQLite drivers.
# Use -dp to override.
driverPath=lib/h2-1.3.173.jar
